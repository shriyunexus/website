import Button from './button/Button';

/**
 * DirectionAwareButton (Backwards Compatibility Alias)
 * Forwards directly to the unified Design System Button component.
 */
export default function DirectionAwareButton(props) {
  return <Button {...props} />;
}
